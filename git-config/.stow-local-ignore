# It's important that .gitignore is absent from this list, so
# that the .gitignore in this repo gets installed by stow as
# ~/.gitignore. Thanks to https://github.com/aspiers/git-config.
# However, .git must be in this list:

\.git
/bin/git-deps
README.md
