#!/bin/bash

if ! [ -x "$(command -v glab)" ]; then
  echo "glab cli is not installed"
  return
fi

glab mr list --author hcramer1 | cat | grep -v 'open merge requests' | awk '{print $NF }' | tr -d '()'
